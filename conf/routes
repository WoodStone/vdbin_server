
# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /bin                           controllers.ApiController.gets(page: Int ?= 1, pageSize: Int ?= 10)
GET     /bin/:id                       controllers.ApiController.get(id)
POST    /bin                           controllers.ApiController.post